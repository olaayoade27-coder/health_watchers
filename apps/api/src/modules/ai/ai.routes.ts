import { Router } from 'express';

const router = Router();

// Placeholder — AI routes to be implemented
router.get('/health', (_req, res) => res.json({ status: 'ok', service: 'ai' }));

export default router;
